spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/kassa
    username: kassa
    password: kassa

app:
  jwt:
    secret: local-only-secret-key-at-least-32-bytes!!
  cors:
    origins:
      - http://localhost:3000
